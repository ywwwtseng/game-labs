{
  "name": "game-labs",
  "repository": "git@github.com:ywwwtseng/game-labs.git",
  "scripts": {
    "dev": "yarn dev:app & yarn dev:builder",
    "dev:app": "cd app && yarn dev",
    "dev:builder": "cd builder && yarn dev",
    "prettier": "npx prettier --write \"**\/*.js\" --single-quote --print-width 80"
  }
}
